pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Build steps added with Jenkins'
            }
        }
        stage('Test') {
            steps {
                echo 'Test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy'
            }
        }
    }
}
